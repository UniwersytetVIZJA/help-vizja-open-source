{% extends 'admin/_layouts/after-login.html.twig' %}
{% form_theme form 'student/_layouts/form-tailwind.html.twig' %}

{% block stylesheets %}{{ encore_entry_link_tags('admin_default') }}{% endblock %}
{% block javascripts %}{{ encore_entry_script_tags('admin_default') }}{% endblock %}
{% block title %}{% trans %}Dodawanie typu sprzętu{% endtrans %} | Admin Help VIZJA{% endblock %}
{% block body %}
    <h1 class="text-2xl md:text-3xl font-bold mb-4">{% trans %}Dodaj typ sprzętu{% endtrans %}</h1>

    {% if item is defined %}
        <div class="mb-4 text-sm text-zinc-600 dark:text-zinc-300">
            {% trans %}Kategoria{% endtrans %}: <span
                class="font-semibold text-zinc-900 dark:text-white">{{ item.value ?? item }}</span>
        </div>
    {% endif %}

    {{ form_start(form, { attr: { novalidate: 'novalidate' } }) }}

    {# Błędy ogólne #}
    {{ form_errors(form) }}

    <div class="space-y-4">
        <div class="max-w-2xl space-y-4 w-full">
            {{ form_row(form.type) }}
        </div>
    </div>

    <div class="mt-6 flex gap-2">
        <button class="{{ buttonPrimary() }} {{ focusPrimaryWithPadding() }}" type="submit">
            {% trans %}Zapisz{% endtrans %}
        </button>

        {% if item is defined %}
            <a
                class="{{ buttonSecondary() }} {{ focusPrimaryWithPadding() }}"
                href="{{ path('admin_inventory_equipment', { item: item.id }) }}"
            >
                {% trans %}Anuluj{% endtrans %}
            </a>
        {% endif %}
    </div>

    {{ form_end(form) }}
{% endblock %}
